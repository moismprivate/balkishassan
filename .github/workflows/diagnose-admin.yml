name: Diagnose admin circuit

on:
  workflow_dispatch:

jobs:
  logs:
    runs-on: ubuntu-latest
    steps:
      - name: Configure SSH
        shell: bash
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Read recent application exceptions
        shell: bash
        run: |
          ssh "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" \
            "journalctl -u balkis --since '1 hour ago' --no-pager -o cat 2>&1 | grep -E 'Unhandled exception|Exception:|Exception |^   at |^      at |fail: Microsoft.AspNetCore.Components.Server.Circuits|System\\.|insufficient permissions' | tail -n 120; sudo -n systemctl status balkis --no-pager -n 80 2>&1 | grep -E 'Exception|Error|Circuit|Active:|not allowed|password' | tail -n 80"
